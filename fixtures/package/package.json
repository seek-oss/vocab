{
  "name": "@vocab-fixtures/package",
  "private": true,
  "scripts": {
    "build": "tsdown",
    "compile": "vocab compile"
  },
  "dependencies": {
    "@vocab/core": "workspace:^"
  },
  "devDependencies": {
    "@vocab/cli": "workspace:^",
    "@vocab/rollup-plugin": "workspace:^",
    "tsdown": "^0.16.0"
  }
}
