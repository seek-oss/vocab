{
  "name": "@vocab-fixtures/vite-ssr",
  "version": "0.0.1",
  "author": "SEEK",
  "private": true,
  "scripts": {
    "compile": "vocab compile",
    "build": "vocab compile && vite build && vite build --ssr",
    "start": "vocab compile && vite build && vite build --ssr && node dist/server/server.cjs"
  },
  "dependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^24.0.0",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vocab/cli": "workspace:*",
    "@vocab/core": "workspace:*",
    "@vocab/react": "workspace:*",
    "@vocab/vite": "workspace:*",
    "express": "^5.1.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "vite": "^8.0.0"
  }
}
